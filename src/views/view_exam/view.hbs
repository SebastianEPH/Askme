<div class="container p-4">
    {{#if data}}
        {{#if all}}
            <h1 style="text-align: center">Todos los examenes<span class="badge bg-secondary"></span></h1>
        {{else}}
            <h1 style="text-align: center">Mis Exámenes<span class="badge bg-secondary"></span></h1>
        {{/if}}
        <br>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">

        <div class="portfolio">
            <div class="container">
                <div class="row">
                    {{#each data}}
                       <div class="col-sm-6">
                           <div class="card-header">
                               <ul class="nav nav-tabs card-header-tabs">
                                   <li class="nav-item">
                                       <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">{{category cat_id}}</a>
                                   </li>
                                   <li class="nav-item">
                                       <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">{{level lev_id}}</a>
                                   </li>

                               </ul>
                           </div>
                           <div class="card">
                               <div class="card-body">
                                   <h5 class="card-title">{{title}}</h5>
                                   <p class="card-text">{{commentary}}</p>
                                   <a href="#" class="btn btn-primary">Tomar Exámen</a>
                                   <a href="#" class="btn btn-primary">Editar Exámen</a>
                                   <a href="#" class="btn btn-primary">Eliminar</a>
                               </div>
                           </div>
                           <div class="card-footer text-muted">
                               2 days ago
                           </div>
                       </div>
                {{/each}}
                </div>

            </div>
        </div>

    {{else}}
        <h1>No se encontró ninguna pregunta<span class="badge bg-secondary"></span></h1>
    {{/if}}


</div>